{{!-- ***************************************************** ADDING COMMENT TO POST VIEW *************************************************** --}}
<section class="hero">
    <div class="hero-body">
        <div class="container">
            <section class="section">
                <div class="columns">
                    <div class="column is-8 is-offset-2">
                       <div class="content is-medium">
            <h1 style="font-size: 225%; text-align: center; font-weight: bold; border-bottom: solid 0.5px black; color: black" class="title">{{postData.title}}</h1>
            <h2 style="text-align: center; font-size: 100%; color: black">{{format_date postData.createdAt}}</h2>
            <h6 style="text-align: center; color: black" >by {{postData.user.username}}</h6>
            <img id="post-image" style="border: dashed 1.25px black" id="photo_Url" src="{{postData.photo_Url}}">
           </div>
                </div>
                </div>
            </section>
<div class="is-divider"></div>
            <div class="column is-8 is-offset-2">
                <div class="field is-horizontal">
                    <div class="field-label is-normal">
                         <label style="text-align: center; font-size: 150%; color: whitesmoke" class="label">Comment: 📢</label>
                    </div>
                    <div class="field-body">
                        <div class="field">
                            <div style="margin-top: 1%" class="control">
                                <textarea
                                    class="textarea comment-input"
                                    placeholder="Enter your pill related information here...💊"
                                    required/>
                                </textarea>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="field is-horizontal">
                    <div class="field-label">
                        <!-- Left empty for spacing -->
                    </div>
                    <div class="field-body">
                        <div class="field">
                            <div class="control">
                                <button id="comment-submit" class="button is-success comment-submit">
                                    Submit 📬
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
<div class="container" style="margin-top: 5%; border: solid 2px whitesmoke">
    <h1 style="font-weight: bold; text-align: center; font-size: 150%; color: whitesmoke">Pill Image Match</h1>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>
            <div style="margin-left: 15%; padding: 2%" class="form-group dropdown">
                <label style="font-weight: bold; color: black; font-size: 115%" for="type" value="">Type 🧪:</label>
                <select style="font-size: 110%" id="dropdown" name="dropdown">
                    <option>select...</option>
                    <option value="capsule">capsule</option>
                    <option value="tablet">tablet</option>
                    <option value="caplet">caplet</option>
                    <option value="gelcap">gelcap</option>
                </select>
            </div>
            <div style="padding: 2%" class="form-group dropdown">
                <label style="font-weight: bold; color: black; font-size: 115%" for="color" value="">Color 🩸:</label>
                    <select style="font-size: 110%" id="dropdown" name="dropdown">
                    <option>select...</option>
                    <option value="white">white</option>
                    <option value="blue">blue</option>
                    <option value="yellow">yellow</option>
                    <option value="red">red</option>
                    <option value="pink">pink</option>
                    <option value="orange">orange</option>
                </select>
            </div>
            <div style="padding: 2%" class="form-group dropdown">
                <label style="font-weight: bold; color: black; font-size: 115%" for="shape" value="">Shape 🔬:</label>
                <select style="font-size: 110%" id="dropdown" name="dropdown">
                    <option>select...</option>
                    <option value="round">round</option>
                    <option value="oblong">oblong</option>
                    <option value="oval">oval</option>
                    <option value="rectangular">rectangular</option>
                    <option value="triangular">triangular</option>
                </select>
                </div>

    <div id="image">
        <img style="margin-left: 35%; border: dashed 1px black" src="https://res.cloudinary.com/dhqsixgmo/image/upload/v1664851514/pill-git-it-images/white-tab_m8srwd.jpg">
    </div>
    <script>
$(document).ready(function() {
  $('#dropdown').on('change', function() {
   if (['capsule' , 'white' , 'round'].indexOf(this.value) > -1)
    {
      $("#image").show();
    }
    else 
    {
      $("#image").hide();
    }
    })
    .change();
    });
</script>
</div>
            </div>
        </div> 
    </div>        
</section> 
{{!-- ***************************************************** STRING OF COMMENTS FOR EACH POST *************************************************** --}}
            <div class="column is-8 is-offset-2" id="comment-section">
                {{#each postData.comments as |cmt|}}
                    <article class="message">
                        <div class="message-body">
                            {{comment}}
                            <p class="has-text-right"><strong>-
                                {{user.username}},
                                {{format_date createdAt}}</strong>
                                {{#if ../loggedInUserData.isAdmin}}
                                (<span class="delete-comment" id="delete-comment" data-id="{{id}}">❌</span>)</p>
                                {{/if}}
                        </div>
                    </article>
                {{/each}}
            </div>
        </div>
    </div>
    <div class="current-post-id is-invisible">{{postData.id}}</div>
</section>
  <script src="/js/post.js"></script>