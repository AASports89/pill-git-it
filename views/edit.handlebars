<section class="hero has-background-grey-lighter">
    <div class="hero-body">
        <div class="container">
            <div class="columns is-centered">
                <div class="column">
                    <form action="" class="box">
                        <label class="label is-large" for="" class="label">Edit Pill Data ✍️</label>
                        <div class="field">
                            <label for="" class="label">Title</label>
                            <div class="control">
                                <input type="text" class="input subject-input" value="{{postData.title}}" required/>
                            </div>
                        </div>
                        <div class="field-body">
                            <div class="field">
 <div class="card" id="photo_Url" style="width: 18rem;">
  <figure id="#photo_Url">{{postData.photo_Url}}</figure>
  <div class="card-body">
   <button id="upload_widget" class="cloudinary-button">Upload Image 📥</button>
<script src="https://upload-widget.cloudinary.com/global/all.js" type="text/javascript"></script>  
            <script type="text/javascript">  
              var myWidget = cloudinary.createUploadWidget({
                cloudName: 'dhqsixgmo', 
                  uploadPreset: 'dpfyatxo'}, (error, result) => { 
                  if (!error && result && result.event === "success") { 
                      console.log('Done! Here is the image info: ', result.info); 
                    }
                  })
document.getElementById("upload_widget").addEventListener("click", function(){
    myWidget.open();
  }, false);
</script>
        <div class="row mb-3">
        <div class="col">
            <input type="hidden" name="_method" value="PUT">
            <div class="form-group dropdown">
                <label for="type" value="">Select Type</label>
                <select class="form-control" id="type-input" name="type">
                      {{#each type}}
                    <option>{{capsule}}</option>
                    <option>{{tablet}}</option>
                    <option>{{caplet}}</option>
                    <option>{{gelcap}}</option>
                    {{/each}}
                </select>
            </div>
        </div>
        <div class="col">
            <div class="form-group dropdown">
                <label for="color" value="">Select Color</label>
                <select class="form-control" id="color-input" name="color">
                    {{#each color}}
                    <option>{{white}}</option>
                    <option>{{blue}}</option>
                    <option>{{yellow}}</option>
                    <option>{{red}}</option>
                    <option>{{pink}}</option>
                    <option>{{orange}}</option>
                    {{/each}}
                </select>
            </div>
     <div class="col">
            <div class="form-group dropdown">
                <label for="imprint" value="">Select Imprint</label>
                <select class="form-control" id="imprint-input" name="imprint">
                    {{#each imprint}}
                    <option>{{3e4}}</option>
                    <option>{{t12}}</option>
                    <option>{{l03}}</option>
                    <option>{{s20}}</option>
                    <option>{{e45}}</option>
                    <option>{{p12}}</option>
                    <option>{{s07}}</option>
                    {{/each}}
                </select>
            </div>
     <div class="form-group dropdown">
                <label for="description" value="">Select Description</label>
                <select class="form-control" id="description-input" name="description">
                    {{#each description}}
                    <option>{{round}}</option>
                    <option>{{oblong}}</option>
                    <option>{{rectangular}}</option>
                    {{/each}}
                </select>
            </div>
        </div>
    </div>
                                </div>
                            </div>
                        </div>
                        <div class="field is-grouped is-grouped-right mt-3">
                            <button class="button is-success edit-submit">
                                Submit 📥
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
    <div class="current-post-id is-invisible">{{postData.id}}</div>
</section>
<script src="/js/edit.js"></script>